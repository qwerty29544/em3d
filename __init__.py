# Воспроизводимые эксперименты главы 4: решатели, поле, ЭПР и задача Ми

## Назначение

Стенд `experiments.em_validation` реализует единый экспериментальный протокол для
последующих разделов главы 4. Он использует дискретный электродинамический оператор
пакета `em3d` и не содержит независимой реализации интегрального ядра или решателей.

Протокол разделён на четыре серии:

- **V1** — сравнение SIM, BiCGStab и TwoStep по истинной относительной невязке,
  времени и числу прямых и сопряжённых действий оператора;
- **V2** — количественное сопоставление найденных векторных полей, построение
  центральных срезов полного модуля, модуля и фазы поляризационной компоненты с
  общей цветовой шкалой, а также карт разности решений;
- **V3** — сравнение абсолютных и нормированных диаграмм эффективной поверхности
  рассеяния;
- **V4** — независимая верификация на однородном изотропном шаре по решению Ми.

## Научные ограничения протокола

1. Решение признаётся пригодным для физической постобработки только при одновременном
   выполнении внутреннего критерия решателя и независимой проверки
   `||Au-f||/||f|| <= rtol`.
2. Несошедшийся запуск сохраняется как диагностический результат, но не используется
   при сравнении полей и ЭПР. В публикационной конфигурации это явно проверяется на
   случае `local_inclusion_stress`: параметр SIM определяется по недостаточно подробной
   грубой сетке, после чего стационарная итерация расходится на рабочей сетке, тогда как
   сошедшиеся BiCGStab и TwoStep остаются доступными для физической постобработки.
3. SIM получает параметр по отдельной грубой дискретизации непрерывно заданного
   физического случая; материальный тензор рабочей сетки не прореживается.
4. По оси вычислительной работы используется суммарное число действий `A` и `A*`.
   Это необходимо, поскольку одна итерация TwoStep дороже одной стационарной итерации.
5. Для задачи Ми аналитические характеристики вычисляются для номинального радиуса
   и для эффективного радиуса

   `a_eff = (3 V_h / (4 pi))^(1/3)`,

   восстановленного по объёму воксельного тела. Так разделяются геометрическая и
   алгебраическая составляющие ошибки.
6. Сопоставляются нормированная форма ЭПР, абсолютный масштаб, интегральная
   характеристика и угловое положение глобальных максимумов. Для симметричных диаграмм
   сравнивается множество эквивалентных максимумов, а не единственный индекс `argmax`.
   Совпадение одной нормированной диаграммы не считается полной верификацией.
7. Для поля Ми сохраняются раздельные ошибки полного и рассеянного полей, в том числе
   ошибка рассеянной компоненты вне сферы. Полное поле вне слабоконтрастного тела может
   быть искусственно близким из-за доминирования падающей волны, поэтому одной метрики
   полного поля недостаточно.

## Пакетный API

Повторно используемые численные операции находятся в
`src/em3d/experiments/stationary_validation.py`:

- `estimate_parameter_circle`;
- `run_solver_comparison`;
- `true_relative_residual`;
- `compare_fields`;
- `compute_rcs_curve`;
- `compare_rcs_curves`;
- `evaluate_mie_solution`.

Верхнеуровневый воспроизводимый сценарий находится в
`experiments/em_validation/workflow.py` и вызывается функцией
`run_validation_suite`.

## Запуск

Быстрая регрессионная серия:

```bash
python -m experiments.em_validation.cli \
  --mode quick \
  --device cpu \
  --study all \
  --output-root experiments/outputs/em_validation_quick
```

Итоговая серия:

```bash
python -m experiments.em_validation.cli \
  --mode publication \
  --device cuda \
  --study all \
  --output-root experiments/outputs/em_validation_publication
```

Отдельно можно выполнить только `solver` или `mie`.

## Артефакты

Каждый запуск создаёт `manifest.json`, `config.json`, таблицы CSV, необработанные
истории невязок и диаграммы ЭПР, а также рисунки PNG/PDF/SVG. Основные таблицы:

- `solver_parameter_circles.csv`;
- `solver_runs.csv`;
- `field_comparisons.csv`;
- `rcs_solver_comparisons.csv`;
- `mie_solver_runs.csv`;
- `mie_validation.csv`;
- `mie_validation_selected.csv`;
- `FINAL_experimental_protocol.csv`.

Полные трёхмерные решения серии Ми после вычисления метрик освобождаются из памяти.
Для воспроизводимой визуализации сохраняются компактные центральные срезы полного и
рассеянного поля, а также численные и аналитические диаграммы ЭПР.

Схема манифеста: `em3d-stationary-validation-artifacts-v1`.

## Блокнот и приложение к диссертации

Тонкий блокнот находится в
`notebooks/em-solver-farfield-validation.ipynb`. Он только импортирует пакетный API,
задаёт конфигурацию и показывает уже сформированные артефакты.

Кодовые ячейки для печатного приложения экспортируются командой:

```bash
python tools/export_notebook_code.py \
  notebooks/em-solver-farfield-validation.ipynb \
  appendix/em-solver-farfield-validation.py
```
